export enum CardPointValueEnum {
  Full = 3,
  Third = 1,
  None = 0,
}

export enum CardSuitEnum {
  Coins = 'COINS',
  Swords = 'SWORDS',
  Cups = 'CUPS',
  Clubs = 'CLUBS',
}

export enum SessionTypeEnum {
  New = 'NEW',
  Join = 'JOIN',
}

export enum PlayerEnum {
  Player1 = 'PLAYER1',
  Player2 = 'PLAYER2',
}
